[package]
name = "vrl-examples-probe"
version = "0.0.0"
edition = "2021"

[dependencies]
# Pinned to the exact rev vector-bindings resolves, so the extracted examples
# match the engine under test.
vrl = { git = "https://github.com/vectordotdev/vrl.git", rev = "f5bd183", default-features = false, features = ["stdlib"] }
serde_json = "1"

[workspace]
